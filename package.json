{
  "name": "@crugo/connection-remit",
  "version": "2.0.0",
  "description": "Connect to RabbitMQ instances via Remit using a simplified, synchronous interface",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jack Williams <jack@wildfire.gg>",
  "license": "MIT",
  "dependencies": {
    "remit": "^2.0.0-beta.3",
    "remit-influxdb": "0.0.3"
  }
}
